# 最终交付验收记录 - 2026-06-17

> 历史记录提示（2026-06-18）：本文记录 2026-06-17 当时的 clean worktree 验收结果。当前最新本地交付结论见 `docs/product/最终交付验收记录-20260618.md`。

## 交付目标

当前交付目标是 **DXM 半托管自动化工作台**：用户通过桌面 Agent Console 打开真实店小秘浏览器，按编辑页分区补齐配置，创建单商品只保存任务，在真实只读检查和人工批准通过后执行受控 `single_save`，并在报告中核对保存成功且未发布。

当前软件交付范围是桌面自动化工作台 + 受控单商品只保存。2026-06-17 11:49（Asia/Shanghai）clean worktree 验收中真实 DXM 写入为 `READY`，范围仅限 `controlled_single_save_only`。`claim_only`、`batch_save`、批量无人值守和任何发布动作仍未放行，不能复用单商品只保存证据扩大解释。

## 当前可交付入口

推荐给真实用户的桌面目录免安装版：

```text
C:\Users\wz\Desktop\DXM-Agent-Console-免安装版\DXM-Agent-Console.exe
```

仓库内同源目录版：

```text
outputs\desktop-build\win-unpacked\DXM-Agent-Console.exe
```

单文件 portable：

```text
outputs\desktop-build\DXM-Agent-Console-Portable-0.1.0.exe
```

目录版必须保留整个文件夹和 `resources` 目录。portable 首次启动需要解包，`%TEMP%` 所在磁盘建议至少保留 1GB 可用空间。

## 验收命令与结果

最终验收命令：

```bat
scripts\final-delivery-check.bat -RequireCleanWorktree -CheckPortableDesktop -ExpectedRealDxmWriteReadiness READY
```

实际验收输出目录：

```text
outputs\final-delivery-check
```

结果：

- `ok=true`
- `okScope=local_workbench_and_controlled_single_save_ready`
- `Local workbench check: PASS`
- `Browser QA: PASS`
- `Final report center QA: PASS`
- `Source package check: PASS`
- `Source package readiness: CLEAN`
- `Real DXM write readiness: READY`
- `realDxmMutationAllowed=true`
- `realDxmMutationScope=controlled_single_save_only`
- `controlledSingleSaveReady=true`
- `batchUnattendedPublishAllowed=false`
- 后端全量测试：`598 passed`
- Git HEAD: `41f803acc0f3ef4f2242faca77cc5d1bcd76d084`

报告文件：

```text
outputs\final-delivery-check\final-delivery-check.md
outputs\final-delivery-check\final-delivery-check.json
```

## 本次修复覆盖

- 真实店小秘浏览器已经登录时，`DxmWorkflowAdapter.check_login_state()` 优先复用可见登录浏览器状态，不再因为二次同步 Playwright probe 抛出 greenlet / asyncio loop 错误而误报登录失败。
- 失败任务卡片读取真实 job/report 错误，用户能看到“浏览器连接异常”等可处理原因，而不是固定显示“上次保存没有完成”。
- 当时目录免安装版已覆盖到 `C:\Users\wz\Desktop\DXM-Agent-Console-免安装版`，并通过 packaged/portable smoke。

## 当前边界

- 当前 READY 只支持受控单商品只保存。
- 真实 `claim_only`、`batch_save`、批量无人值守和任何发布动作仍未放行。
- 扩大范围前必须重新建立对应模式的 L2/L3 证据链、人工批准策略、失败隔离和回滚方案。
- 真实店小秘页面变化后，必须重新跑可见浏览器 L2 复验，不能靠旧截图、本地演示数据或历史金丝雀扩大解释。
