# 最终交付验收记录 - 2026-06-22

> **历史验收记录。** 结论只适用于本文记录的日期、包和 `controlled_single_save_only` 范围，不是 2026-07-17 当前源码的 READY 证明。当前结论见 [2026-07-17 状态说明](DXM-Agent-Console-当前开发状态与后续计划-20260717.md)。

## 交付目标

当前交付目标是 **DXM 单商品只保存 Agent Console**：用户通过免安装桌面 EXE 打开 Agent 控制台，登录真实店小秘，按编辑页分区补齐配置，创建单商品只保存任务，在真实只读检查和人工批准通过后执行受控 `single_save`，并在结果页核对保存成功且未发布。

当前软件交付范围是桌面自动化工作台 + 受控单商品只保存。`claim_only`、`batch_save`、批量无人值守和任何发布动作仍未放行，不能复用单商品只保存证据扩大解释。

## 当前可交付入口

当前给用户的免安装 EXE：

```text
D:\Desktop\DXM-Agent-Console-免安装版\DXM-Agent-Console-Portable-0.1.0.exe
```

文件信息：

- LastWriteTime: `2026-06-22 18:12:24`
- Size: `102055078`
- SHA-256: `24319C572777C40D9B1C3B2D46CD8582859ED0FE834EE204E1FAC51C5E15FFA4`

仓库内同源构建产物：

```text
outputs\desktop-build\win-unpacked\DXM-Agent-Console.exe
outputs\desktop-build\DXM-Agent-Console-Portable-0.1.0.exe
```

## 验收命令与结果

本次本地工作台验收报告：

```text
outputs\final-delivery-check\final-delivery-check.md
outputs\final-delivery-check\final-delivery-check.json
```

结果：

- Checked at: `2026-06-22T10:14:16.4027868Z`（Asia/Shanghai `2026-06-22 18:14:16`）
- `ok=true`
- `status=local_workbench_check_pass`
- `okScope=local_workbench_and_controlled_single_save_ready`
- `Local workbench check: PASS`
- `Browser QA: PASS`
- `Final report center QA: PASS`
- `Source package check: NOT_REQUIRED`
- `Source package readiness: DIRTY`
- `Real DXM write readiness: READY`
- `realDxmMutationAllowed=true`
- `realDxmMutationScope=controlled_single_save_only`
- `batchUnattendedPublishAllowed=false`
- 后端全量测试：`678 passed`
- 交付前 focused suite：`310 passed`
- 前端生产构建：`PASS`
- 桌面生产构建：`PASS`
- packaged desktop smoke：`PASS`
- portable desktop smoke：`PASS`
- 报告内 Git HEAD: `4e555da7080ac1e5423d89ad86a2d290cda446c7`
- 本轮最终提交并推送: `83537919cd3a3f3366caaff69032b1f01231b047`

## 解释边界

- 本次记录证明 2026-06-22 的本地工作台、portable EXE、浏览器 QA、最终报告中心 QA 和受控单商品只保存 READY 均通过。
- 报告内 `Source package readiness: DIRTY` 表示该报告不是 clean worktree 源码发布包验收；它是用户可用 portable 与本地工作台验收。
- 报告内 Git HEAD 是执行最终检查时的提交；随后新增了文档和交付记录提交 `83537919cd3a3f3366caaff69032b1f01231b047` 并推送到 `origin/main`。
- `READY` 只支持受控单商品只保存；真实 `claim_only`、`batch_save`、批量无人值守和任何发布动作仍未放行。
- L2 真实只读证据有新鲜度窗口。再次启动真实保存前，应以当次工作台状态或重新运行的 `final-delivery-check` 为准，不能把历史 READY 当永久授权。

## 后续源码包交付要求

正式源码包交付必须重新运行：

```bat
scripts\final-delivery-check.bat -RequireCleanWorktree -CheckPortableDesktop -ExpectedRealDxmWriteReadiness READY
```

如果当次没有新鲜 L2/L3 证据，应使用 `-ExpectedRealDxmWriteReadiness BLOCKED`，并且不得启动真实保存。
