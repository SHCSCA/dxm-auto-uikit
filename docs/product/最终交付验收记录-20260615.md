# 最终交付验收记录 - 2026-06-15

## 交付目标

当前交付目标是 **DXM 半托管自动化工作台**：用户通过桌面 Agent Console 打开真实店小秘浏览器，按编辑页分区补齐配置，创建单商品只保存任务，在只读页面检查和人工批准通过后执行受控 `single_save`，并在报告中核对保存成功且未发布。

当前软件交付范围是桌面自动化工作台；真实写入状态由 L2/L3 证据动态决定。最新本地验收中真实 DXM 写入因 L2 证据过期保持 `BLOCKED`，这是正确门禁行为。`claim_only`、`batch_save`、批量无人值守和任何发布动作仍未放行，不能复用单商品只保存证据扩大解释。

## 当前可交付入口

推荐给真实用户的桌面目录免安装版：

```text
C:\Users\wz\Desktop\DXM-Agent-Console-免安装版\DXM-Agent-Console.exe
```

仓库内同源目录版：

```text
outputs\desktop-build\win-unpacked\DXM-Agent-Console.exe
```

单文件 portable：

```text
outputs\desktop-build\DXM-Agent-Console-Portable-0.1.0.exe
```

目录版必须保留整个文件夹和 `resources` 目录。portable 首次启动需要解包，`%TEMP%` 所在磁盘建议至少保留 1GB 可用空间。

## 验收命令与结果

最新本地最终验收命令：

```bat
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\final-delivery-check.ps1 -CheckPortableDesktop -ExpectedRealDxmWriteReadiness BLOCKED
```

结果：

- `ok=true`
- `okScope=local_workbench_only`
- `Local workbench check: PASS`
- `Browser QA: PASS`
- `Final report center QA: PASS`
- `Source package check: NOT_REQUIRED`（正式源码包交付时必须重新跑 `-RequireCleanWorktree`）
- `Real DXM write readiness: BLOCKED`
- `realDxmMutationAllowed=false`
- `realDxmMutationScope=none`
- `controlledSingleSaveReady=false`
- `batchUnattendedPublishAllowed=false`
- Git HEAD: 以 `outputs\final-delivery-check\final-delivery-check.json` 的 `gitHead` 字段为准。

报告文件：

```text
outputs\final-delivery-check\final-delivery-check.md
outputs\final-delivery-check\final-delivery-check.json
```

## L2/L3 证据

L2 真实双目标只读复验：

- run id: `l2-real-20260615T110011Z`
- `data_acquisition`: passed
- `draft_box`: passed
- 两个目标写请求、非只读请求、拦截请求、禁用关键词请求、WebSocket 计数均为 0。

L3：

- 受控单商品 `single_save` 历史金丝雀证据通过。
- 保存响应成功，报告 `published=false`。
- 当前 L2 证据已过期时，该历史证据不能单独放行真实写入；必须先重新取得新鲜 L2 双目标证据，再按 L3 人工批准流程启动。
- 该证据只支持受控单商品只保存，不支持认领、批量、无人值守或发布。

## 当前仍需继续优化的问题

- UI 信息密度仍需要继续压缩，尤其是配置中心和执行控制台的辅助说明要尽量折叠到右侧抽屉或详情区。
- 用户入口必须始终指向真实存在的 exe，文档和桌面目录要同步。
- 扩展到 `claim_only` 或 `batch_save` 前，必须建立对应模式的 L2/L3 证据链、人工批准策略、失败隔离和回滚方案。
- 真实店小秘页面变化后，必须重新跑可见浏览器 L2 复验，不能靠旧截图或本地演示数据判断。
