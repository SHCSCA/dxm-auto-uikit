# E2 冻结遗留 + E3 开工记录

> 由 OpenAI GPT（Codex）AI 生成/维护。

> **用户裁定（2026-08-04）**：E2 先维持现状，**不阻塞进入 E3**；E2/G4 问题必须登记。
> **状态**：`E2_DEFERRED`（非 `E2_ACCEPTED`）· **`E3_OPEN`**
> **禁止**：宣称 `MVP_READY` / `PROD_READY`；Path B；发布类动作。

---

## 1. E2 冻结结论

| 项 | 结论 |
|----|------|
| `E2_ACCEPTED` | **否** |
| G0 | 策略 **B**（完整 L0 仍未绿：2026-08-11 最新 **404 failed / 1553 passed**；历史基线 **426 / 1469**） |
| G1 | 有固定点 `09fceb7…`（后续还有 C06/G5 等 commits） |
| G2 | 专项/前端曾绿 |
| G3 | **未完成**（策略 B 未达 0 fail） |
| G4 | **未完成**（真机零写会话未闭环） |
| G5-1 | Gold 哈希已改 |
| 产品交付含义 | 方案/快照能力可作为 **E3 输入依赖** 继续用，但不算 E2 正式关闭 |

---

## 2. 问题登记（必须保留，供后续回填）

### P-E2-01 · 完整 L0 未绿（策略 B 硬门槛）

- 现象：2026-08-11 全仓 pytest 为 **404 failed / 1553 passed**（不劣于历史 426 / 1469，但仍远未全绿）。
- 簇：`test_v1_runner`、旧 claim/single_save、action evidence 等。
- 红线：不得用放宽三铁证/发布门换绿。
- 跟踪：`docs/product/L0-策略B-迁移计划.md`

### P-E2-02 · 真机零写会话分裂（G4）

- `live-status` 常 `logged_in=true`（Cookie 探针）。
- Playwright 页常 `about:blank`；`login/continue` → `login_failed`。
- `draft-reader/shops` → `BROWSER_SESSION_UNAVAILABLE` 或 `DXM_PAGE_REQUIRED`。
- login 单 worker 排队导致 API 超时。
- 用户否决「靠 PowerShell 脚本」作为主路径。
- 要求：工作台 UI 统一会话门面（见下方交办摘要）。

### P-E2-03 · G4 脚本路径弃用

- `scripts/g4-zero-write-after-login.ps1` 仅诊断用，**非**产品主路径。
- Windows PS5.1 中文/UTF-8 曾导致 ParserError（已 ASCII+BOM 修补，仍不作为主方案）。

### P-E2-04 · 指定类目真实零写链不全

- 目标类目 `201273776 / 2621 / 201898401` 的完整 preview/freeze 真机证据不足。
- child 回包/脱敏 fixture 缺口仍在 BLOCKED 历史条目中。

### P-E2-05 · 工作树与历史删除

- 大量既有 `docs/**` deleted 未整理；与 E2 能力并存。
- 未 push 的本地 commits 需负责人自行 `git push`。

### P-E2-06 · 交办 GPT 的会话架构修复（未做完）

摘要见此前「G4 会话问题交办」：统一 Playwright 会话、reason_code、前端连接店小秘闭环到 preview/freeze。
**E3 开工不依赖该修复完成**，但真机 E3 DoD（3 品三铁证）仍需要可用可见会话。

---

## 3. E3 开工范围（合同 §7.4）

### 做

- mode：`batch_draft_save`（与含糊 `batch_save` 区分）
- 输入：冻结 `plan_snapshot` + `product_ids[]` + Path **A only**
- 串行打开编辑页 → 模板优先补差 → **只点「保存」**
- 三铁证、PublishGuard、mutation ledger、UNKNOWN 停批
- 合约测试：mode 门禁、Path B 拒绝、零发布、队列/幂等

### 不做（本阶段）

- Path B / editFromSmt / 半托管第二段
- 发布 / 保存并移入待发布
- 宣称 `MVP_READY`
- 用直调 `add.json` 当主保存路径

### 与 E2 关系

- E3 **消费** E2 已有：`plan_snapshot` / `create_task_from_snapshot` / 本地 plan。
- E2 未关 **不阻止** E3 开发与自动化；**真机 3 品 DoD** 仍需会话可用（P-E2-02）。

---

## 4. E3 验收 DoD（摘录）

- [x] mode 门禁与 Path B 拒绝有测
- [x] 队列串行、失败/UNKNOWN 策略有测
- [x] 零发布 / PublishGuard 有测
- [ ] 真机 ≥3 draft Path A 各三铁证（人工批准）
- [x] 缺证据不得计成功

---

## 5. E3 开工进度（代码）

| 切片 | 状态 | 说明 |
|------|------|------|
| mode 门禁 | 已接线 | `ExecutionMode.BATCH_DRAFT_SAVE`；`ALLOWED/RELEASED` 含 `batch_draft_save`；旧 `batch_save` 仍未放行 |
| Path B 拒绝 | 已接线 | 批准/启动/`runner` 预检均拒 `path=B` |
| plan 授权事实 | 已接线 | `build_batch_draft_save_task_facts` 绑定 snapshot hash + product_ids |
| 商品身份 | 已接线 | frozen canonical / DOM / 编辑前复核统一拒绝纯字母伪 ID，要求至少一位数字 |
| 原子启动 | 已接线 | `/api/tasks/{id}/approve-and-start` 在一个事务内批准、消费授权并 CAS running；独立批准入口拒绝 batch |
| runner Path A | 已接线 | Path A 专用步骤，不进入半托管第二段；串行 jobs；保存派发超时/证据冲突持久化 UNKNOWN；失败停批；唯一 dispatch CAS |
| 三铁证 | 工程合同绿 | 回包 + 页面成功态 + 独立未发布证明；任一缺失不得计成功 |
| 自动化测 | 专项绿 | E3/ledger/action-result/two-stage `160 passed`；E2+E3+PublishGuard `120 passed`；完整 L0 `404 failed / 1553 passed` 仍红 |
| 真机 ≥3 品三铁证 | **未做** | Path A `editor` 与旧 `single_save` 页面合同已按模式隔离，隔离持久 worker 的 SAVE→VERIFY 已绿；但真实浏览器、保存与发布均为 0，且完整 L0/可复现固定点仍阻断；见 `BLOCKED.md` |

## 6. 变更记录

| 日期 | 说明 |
|------|------|
| 2026-08-04 | 用户裁定 E2 先这样、问题记录、直接进入 E3 |
| 2026-08-04 | E3 门禁/授权/runner Path A 首切接线 + 专项测 |
| 2026-08-10 | E3 后端合同补齐：身份、原子启动、Path A 顺序、UNKNOWN、三铁证和重复派发；保持 E3_OPEN，真机未执行 |
| 2026-08-10 | 最后一轮补齐保存派发超时与成功回包证据冲突的 UNKNOWN 不重试合同；完整 L0 仍红，不进入真机或 READY |
| 2026-08-11 | JIT 精确绑定 command/snapshot/job/lease/mode/target；DISPATCHED 崩溃恢复为 UNKNOWN；正式 origin、模式化页面合同、worktree/终态报告和持久 SAVE→VERIFY 测试收口；版本升至 0.1.3，E3 仍 OPEN/BLOCKED |
| 2026-08-19 | 文档复核：E4 控制面、操作审计与桌面启动修复已进入当前工作树；专项测试已复验，但完整 L0、portable 同 HEAD 与真机三商品仍未关闭 |
