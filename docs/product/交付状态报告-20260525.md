# 交付状态报告 - 2026-05-25

## 当前结论

本文保留 2026-05-25 的历史交付状态；当前最新本地交付结论见 `docs/product/最终交付验收记录-20260618.md`。当前源码包验收按 `Local workbench check` 与 `Real DXM write readiness` 分层判定：工作台、桌面版和本地 QA 可交付；真实 DXM 写入必须依赖新鲜 L2/L3 证据才可处于受控 `single_save READY`。截至 2026-06-18 21:28（Asia/Shanghai），最新本地工作台验收已通过，报告目录为 `outputs\final-delivery-check`，Git HEAD 为 `f99dc38b5cfbe79833a9b9c52d8c36e32a739826`，但 `Source package readiness=DIRTY`；正式源码包交付仍需重新运行 clean worktree 验收。当前不可交付的是店小秘真实无人值守批量保存/写入和任何发布动作。

历史结论：2026-05-22 的真实 L2 只读 probe 证据显示 `data_acquisition` 与 `draft_box` 均失败，因此当时真实 `claim_only` / `single_save` / `batch_save` 不允许启动。2026-06-01 曾补齐 L2/L3 并放行过受控 `single_save`；该结论现在只能作为历史证据，不能替代当前新鲜门禁。

## 已完成

- 后端启动门禁已 fail-closed：
  - 真实 `claim_only` / `single_save` / `batch_save` 必须 L2 真实双目标通过。
  - 必须服务端来源的人工批准令牌。
  - 用户 payload 不能伪造 `manual_approval`。
  - 已完成任务不能重复启动。
  - 直连 `/api/dxm/draft-box/action`、`/api/dxm/workflow/claim-product` 与 `/api/dxm/workflow/open-editor` 不允许执行真实动作，必须走 task runner 证据链。
  - `/artifacts` 仅暴露公开证据目录，禁止暴露 `sessions`、`sqlite`、`ai` 等本地敏感目录。
  - 本地前端与 CORS 默认收敛到 loopback，避免把交付工具暴露到局域网。
- 工作台 API 已聚合：
  - L0/L1/L2/L3 回归门禁。
  - 按 job 的保存/未发布/network 证据完整性。
  - L2 非 `passed` 时证据等级最高为 `C`。
  - L3 在 L2 未通过时明确 `blocked`。
- 前端工作台已具备：
  - L2/L3 阻断状态提示。
  - 真实保存按钮禁用策略。
  - 空状态、接口降级状态和操作错误提示。
  - L2 失败诊断摘要，展示最终路径、失败硬门禁、被拦截请求聚合和只读依赖候选。
- L2 probe 已输出：
  - `strict_pass_checks`
  - `navigation`
  - `render_state`
  - `blocked_request_groups`
  - `allowlist_review_candidates`

这些诊断只解释失败原因，不改变放行条件。

## 真实 L2 证据

证据目录：`data/l2_readonly_probe/real_20260522/`

| target | ok | final_url | write | non_read | blocked | forbidden | 结论 |
| --- | --- | --- | ---: | ---: | ---: | ---: | --- |
| `data_acquisition` | false | `/web/home` | 1 | 1 | 21 | 4 | 失败 |
| `draft_box` | false | `/web/home` | 0 | 0 | 25 | 5 | 失败 |

当前失败类别：

- 目标可达性不足：两个目标最终都回到 `/web/home`，不是目标页面。
- SPA 启动请求被严格只读策略拦截：如 `userInfo.json`、`getGrayModule.json` 等 active XHR。
- 后台风险信号存在：`publishDetection` 命中禁词，`data_acquisition` 还有第三方 POST 事件请求。

> 2026-06-18 更新提示：本文保留历史验收状态。当前产品身份已统一为 DXM 半托管自动化工作台；历史 L2/L3 证据只能证明当时通过。当前真实 DXM 写入 readiness 应按最新 `final-delivery-check` 结果判定；没有新鲜 L2/L3 时应为 `BLOCKED`，具备新鲜 L2/L3 且报告范围为 `controlled_single_save_only` 时才可视为受控 `READY`。

## 已验证

- DXM 半托管自动化工作台最新本地工作台自检已按 `scripts\final-delivery-check.ps1 -CheckPortableDesktop -ExpectedRealDxmWriteReadiness READY` 通过；该 READY 只覆盖 `controlled_single_save_only`。源码正式交付仍需重新运行 clean worktree 验收。
  - Local workbench check：`PASS`
  - Real DXM write readiness：`READY`
  - Real DXM mutation scope：`controlled_single_save_only`
  - Source package readiness：`DIRTY`
  - Git HEAD：`f99dc38b5cfbe79833a9b9c52d8c36e32a739826`
  - 后端全量测试：`674 passed`
  - L1 selector replay：`3/3 passed`
  - 浏览器 QA：无 console error、无网络失败、无 4xx/5xx、无非 GET、无外部 origin，桌面/移动页面截图写出，移动端无横向溢出
  - 直连阻断 QA：本地任务启动、执行控制台启动、`/api/dxm/draft-box/action`、`/api/dxm/workflow/claim-product` 与 `/api/dxm/workflow/open-editor` 均返回 403，且任务状态未被修改
- 后端全量测试：当前新增 CORS、artifact 暴露、最终自检摘要和直连真实动作拒绝回归；以最新 `final-delivery-check` 输出为准。
- 前端生产构建：`npm run build` 通过

## 未完成 / 阻断

- 受控单商品 `single_save` 已可交付给内部验收。
- 真实 `claim_only` / `batch_save`、批量无人值守和发布尚未放行。
- 源码发布包干净度上次已在 2026-06-17 11:49（Asia/Shanghai）通过 `-RequireCleanWorktree` 验收；当前工作区已有后续改动，后续发布前必须重新运行。

## 下一步

1. 保持 `config/l2_readonly_allowlist.json` 的最小只读范围，并定期复核 DXM 页面启动依赖。
2. 源码发布包前运行 `scripts\final-delivery-check.ps1 -RequireCleanWorktree -CheckPortableDesktop -ExpectedRealDxmWriteReadiness READY`，并确认报告范围仍为 `controlled_single_save_only`；如果当次没有新鲜 L2/L3，则改用 `BLOCKED` 且不得启动真实保存。
3. 若要扩大到 `claim_only` / `batch_save`，重新建立对应 L2/L3 证据链。
4. 批量、无人值守和发布必须单独设计门禁与人工批准策略。
